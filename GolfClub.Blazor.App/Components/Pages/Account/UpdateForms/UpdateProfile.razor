@using GolfClub.BLL.Models.Domain

<h2 class="text-success mb-3">Update Personal Information</h2>
<p class="mb-4">Update your personal details here</p>

<EditForm Model="@model" OnValidSubmit="HandleValidSubmit">
    <DataAnnotationsValidator />
    <div class="row">
        <div class="mb-5">
            <label for="first-name" class="form-label">First Name</label>
            <InputText id="first-name" class="form-control" @bind-Value="model.FirstName" />
        </div>
        <div class="mb-5">
            <label for="last-name" class="form-label">Last Name</label>
            <InputText id="last-name" class="form-control" @bind-Value="model.LastName" />
        </div>
    </div>
    <div class="row">
        <div class="mb-5">
            <label for="dob" class="form-label">Date Of Birth</label>
            <InputText id="dob" class="form-control" @bind-Value="model.DateOfBirth" />
        </div>
        <div class="mb-5">
            <label for="address" class="form-label">Physical Address</label>
            <InputText id="address" class="form-control" @bind-Value="model.Address" />
        </div>
    </div>
    <div class="row">
        <div class="mb-5">
            <label for="golfclub" class="form-label">Golf Club Size</label>
            <InputText id="golfclub" class="form-control" @bind-Value="model.GolfClubSize" />
        </div>
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
</EditForm>

@code {

    [SupplyParameterFromForm]
    private UpdateProfileModel model { get; set; }

    private Task HandleValidSubmit()
    {
        // todo: implement this
        return null;
    }
}
